For Backend 
This is how build docker image 
docker build -t mern-backend .

This is how run docker image 
docker run -p 8080:8080 --name backend --env-file .env mern-backend

----------------------------------------------------------------------------------------------------------------------

For Frontend
This is how build docker image 
docker build -t mern-frontend .

This is how run docker image 
docker run -p 5173:5173 --name frontend --env-file .env mern-frontend